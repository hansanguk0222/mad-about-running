import { Calendar } from '@/components/ui/calendar';
import { TrainningSummary } from '@/components/ui/trainningSummary';

const CalendarPage = () => (
  <div className="w-full p-8">
    <TrainningSummary />
    <Calendar />
  </div>
);

export default CalendarPage;
